@page
@model WebApp.Pages.EmployeeDeptHistorys.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EmployeeDeptHistory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EmpDeptHistItem.DepartmentId" class="control-label"></label>
                <select asp-for="EmpDeptHistItem.DepartmentId" class="form-control" asp-items="Model.DepartmentNameSL"></select>
                <span asp-validation-for="EmpDeptHistItem.DepartmentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Start Date</label>
                <input asp-for="EmpDeptHistItem.FromDate" class="form-control" type="date" />
                <span asp-validation-for="EmpDeptHistItem.FromDate" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="EmpDeptHistItem.Id" />
            <input type="hidden" asp-for="EmpDeptHistItem.ApplicationUserId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index" asp-route-usrId="@Model.EmpDeptHistItem.ApplicationUserId">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
