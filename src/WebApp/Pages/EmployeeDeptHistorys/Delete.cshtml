@page
@model WebApp.Pages.EmployeeDeptHistorys.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>EmployeeDeptHistory</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Username
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeDeptHistory.ApplicationUser.UserName
        </dd>
        <dt class="col-sm-2">
            Department
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeDeptHistory.Department.Name
        </dd>
        <dt class="col-sm-2">
            StartDate
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeDeptHistory.FromDate
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="EmployeeDeptHistory.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index" asp-route-usrId="@Model.EmployeeDeptHistory.ApplicationUserId">Back to List</a>
    </form>
</div>
