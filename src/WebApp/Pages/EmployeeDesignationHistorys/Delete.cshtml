@page
@model WebApp.Pages.EmployeeDesignationHistorys.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>EmployeeDesignationHistory</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Username
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeDesignationHistory.ApplicationUser.UserName
        </dd>
        <dt class="col-sm-2">
            Designation
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeDesignationHistory.Designation.Name
        </dd>
        <dt class="col-sm-2">
            StartDate
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeDesignationHistory.FromDate
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="EmployeeDesignationHistory.Id" />
        <input type="hidden" asp-for="EmployeeDesignationHistory.ApplicationUserId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index" asp-route-id="@Model.EmployeeDesignationHistory.ApplicationUserId">Back to List</a>
    </form>
</div>
