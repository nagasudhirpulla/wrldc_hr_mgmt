@page
@model WebApp.Pages.EmployeeBossHistorys.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete Reporting Officer</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>EmployeeBossHistory</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Username
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeBossHistory.ApplicationUser.UserName
        </dd>
        <dt class="col-sm-2">
            Reporting Officer
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeBossHistory.ApplicationUser.BossUserId
        </dd>
        <dt class="col-sm-2">
            Start Date
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeBossHistory.FromDate
        </dd>
        <dt class="col-sm-2">
            To Date
        </dt>
        <dd class="col-sm-10">
            @Model.EmployeeBossHistory.ToDate
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="EmployeeBossHistory.Id" />
        <input type="hidden" asp-for="EmployeeBossHistory.ApplicationUserId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index" asp-route-usrId="@Model.EmployeeBossHistory.ApplicationUserId">Back to List</a>
    </form>
</div>
